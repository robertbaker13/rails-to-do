<h1>Edit <%= @user.username %></h1>

<%= render "form" %>

<%= button_to "Delete User", user_path(@user), method: "delete", data: { confirm: "Are you sure you want to delete #{@user.username}?" } %>

<%= link_to @user.username, user_path(@user) %><br>
<%= link_to "Users", users_path %>